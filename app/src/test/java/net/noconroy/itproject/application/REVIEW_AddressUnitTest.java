package net.noconroy.itproject.application;

/*
import org.junit.Test;
import org.junit.runner.RunWith;
import org.mockito.junit.MockitoJUnitRunner;

import static junit.framework.Assert.assertEquals;
import static org.mockito.Mockito.mock;


Created by Mattias on 12/09/2017.

@RunWith(MockitoJUnitRunner.class)
public class REVIEW_AddressUnitTest {

    @Test
    public void isThisAddressInThisPostcode_SimpleReturnTrue() {

        Address a = mock(Address.class);
        when(a.isThisAddressInThisPostCode("Melbourne", "3400")).thenReturn(true);
        assertEquals(a.isThisAddressInThisPostCode("Melbourne", "3400"), true);
    }

    @Test
    public void isThisAddressInThisPostcode_SimpleReturnFalse() {

        Address a = mock(Address.class);
        when(a.isThisAddressInThisPostCode("Melbourne", "3600")).thenReturn(false);
        assertEquals(a.isThisAddressInThisPostCode("Melbourne", "3600"), false);
    }

    @Test
    public void isThisAddressInThisPostcode_SimpleReturnTrue_AnotherTrue() {

        Address a = mock(Address.class);
        when(a.isThisAddressInThisPostCode("Melbourne", "3300")).thenReturn(true);
        assertEquals(a.isThisAddressInThisPostCode("Melbourne", "3300"), true);
    }
}

*/