package com.example.mattias.devicelocation;

import org.junit.runner.RunWith;

//import org.mockito.junit.MockitoJUnitRunner;

/**
 * Created by Mattias on 11/09/2017.
 */

//@RunWith(MockitoJUnitRunner.class)
public class CompassViewUnitTest {

    /*
    CompassView mCompassView;

    @Before
    public void setUp() {
        mCompassView = mock(CompassView.class);
    }

    @Test
    public void drawCardinalDirection_NorthWithRotationZero_ReturnTrueSimple() {
        when(mCompassView.drawCardinalDirection(0, 0)).thenReturn(true);
        assertEquals(mCompassView.drawCardinalDirection(0, 0), true);
    }

    @Test
    public void drawCardinalDirection_SouthWithRotation150_ReturnTrueSimple() {
        when(mCompassView.drawCardinalDirection(180, 150)).thenReturn(true);
        assertEquals(mCompassView.drawCardinalDirection(180, 150), true);
    }

    @Test
    public void drawCardinalDirection_NorthWithRotation180_ReturnFalseSimple() {
        when(mCompassView.drawCardinalDirection(0, 180)).thenReturn(false);
        assertEquals(mCompassView.drawCardinalDirection(0, 180), false);
    }

    @Test
    public void convertRotation_NoConversionSimple() {
        when(mCompassView.convertRotation(50)).thenReturn((float)50);
        assertEquals(mCompassView.convertRotation(50), (float)50);
    }

    @Test
    public void convertRotation_ConversionSimple() {
        when(mCompassView.convertRotation(-150)).thenReturn((float)210);
        assertEquals(mCompassView.convertRotation(-150), (float)210);
    }
    */
}
